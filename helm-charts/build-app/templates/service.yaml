apiVersion: v1
kind: Service
metadata:
  name: {{ include "build-app.fullname" . }}
  labels:
    {{- include "build-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "build-app.selectorLabels" . | nindent 4 }}
